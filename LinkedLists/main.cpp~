#include "Student.h"
#include "Node.h"
#include <iostream>

using namespace std;

void addNode(Student* student, Node* head){
  
}
void printList(Node* next, Node* head){
  
}
int main(){
  Node* head = NULL;
  add(new Student("Jason Shi", 322023), head);
  add(new Student("Russell Chai", 000000), head);
  add(new Student("Connor Shu", 111111), head);
  add(new Student("Kevin Men", 321491), head);
  print(head);
  
  
}
